.PHONY: install dev test lint check format build clean

install:
	pnpm install

dev:
	pnpm dev --host 0.0.0.0

test:
	pnpm test

lint:
	pnpm lint

check:
	pnpm check

format:
	pnpm format

build:
	pnpm build

clean:
	rm -rf .svelte-kit build node_modules
