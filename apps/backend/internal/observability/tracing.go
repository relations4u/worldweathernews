package observability

// OpenTelemetry-Tracing wird in Session 10 implementiert (Tempo + OTLP).
// Diese Datei hält den Platz, damit die Import-Struktur stabil bleibt.

// Tracing ist der spätere Anker für den OTel-Provider.
type Tracing struct{}
