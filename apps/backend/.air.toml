root = "."
tmp_dir = "tmp"

[build]
bin = "tmp/api"
cmd = "go build -o tmp/api ./cmd/api"
delay = 500
exclude_dir = ["tmp", "vendor", "testdata"]
include_ext = ["go", "yaml"]
stop_on_error = true

[log]
time = true
